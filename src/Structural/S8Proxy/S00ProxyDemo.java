package Structural.S8Proxy;

//Proxies are usually entities that represent other entities,

//Proxy Design pattern does exactly that, it acts as another object
//It is basically a class that serves as an interface between a client application & another object

//When ? ->
// -> When creating a wrapper to cover main's object complexity
// -> When creating a security layer for preventing unauthorized access

public class S00ProxyDemo {
}
