package Creational.S5Prototype;

//Prototype pattern is used whenever your object is too expensive to create
//An example would be to create a very complex object that requires a lot of memory to store all if its properties & methods

//Usually the prototype pattern fits when your object is costly to create, & "cloning" object by copying
// all it's member variables would do the trick
public class PrototypeDemo {
}
